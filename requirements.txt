# — Core ML & Data —
torch>=2.2,<3.0
numpy>=1.26,<2.0
pandas>=2.2,<3.0

# — Baselines & Analysis —
scikit-learn>=1.4,<2.0
scipy>=1.12,<2.0

# — Visualization (scripts / paper figures) —
matplotlib>=3.8,<4.0

# — AU NEM data pipeline —
# NOTE: nemed lives in its OWN Python 3.11 venv (`.venv-nemed311`), NOT here.
# All nemed releases pin pandas<2.0, which conflicts with the pandas>=2.2 above
# (and the rest of this repo). See requirements-nemed.txt and README "AU NEM data".

# — Testing —
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
