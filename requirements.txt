# — Core ML & Data —
torch>=2.2,<3.0
numpy>=1.26,<2.0
pandas>=2.2,<3.0

# — Baselines & Analysis —
scikit-learn>=1.4,<2.0
scipy>=1.12,<2.0

# — Visualization (scripts / paper figures) —
matplotlib>=3.8,<4.0

# — AU NEM data pipeline (optional) —
nemed>=0.2

# — Testing —
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
