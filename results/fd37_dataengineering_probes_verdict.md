# FD-37 verdict:数据工程三探针(用户"还有空间"挑战)—— 全部中性,通道按物理原则保留

**日期**:2026-08-28

## 探针 1:AU ef_nr 常数核对 → 无 bug
QLD/NSW/VIC/SA 硬编码 ef_nr vs NEMED 隐含值(逐 DUID 排放/发电)偏差 ≤0.2% —— 常数本就来自同源数据,嫌疑排除。

## 探针 2:冷锚门近锚定初始化(bias=1.5,镜像 I_0 最优)→ 中性(p=0.96)
VIC1 seed1 −34 不稳;训练在 donor 上反正会把门学回去。**已回退零初始化**。

## 探针 3:EIA-930 interchange 进口通道(全新数据资产)→ 中性
- 提取:8 US 区 × 3 年 net_interchange(EIA-930 第 9 列,此前从未使用;ISNE 均值 −1728 MW=进口)
- 变体 A(去均值 MW,镜像 demand 防漏):p=0.47 中性 —— 去均值恰好丢掉 ISNE 需要的水平信息
- 变体 B(进口份额 interchange/demand,物理量,不 demean):23.74→23.36,p=0.16,趋势正但不显著
- **保留变体 B 通道**(第 18 exog;n_exog 18,hypernet/测试同步):物理上正确、无回归、对中国同构(省间交换份额同量)

## 结论

"还有数据工程空间"的挑战检验完毕:剩余两个最有希望的数据抓手(ef_nr 核对、进口通道)一个无 bug、一个统计中性。基准内数据工程正式穷尽 —— 与 FD-24/30/31/32 的四重水平结论和 FD-35 的形态信息边界判定一致。真实剩余空间在外部数据源(NWP 集合、调度/停机事件、AEMO 注册表补全),均已留档。
